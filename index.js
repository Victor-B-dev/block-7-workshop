/*Pseudo code

Need 4 variables.

One for the string. so const variableName = `string`;
One for each of the numbers. const number = someOperations

Need to put those variables inside the pop up box so they display when accessed.

*/

const codeOne = 6 + 5 * 3 - 11 ;
const codeTwo = 100 / 5 - 13 + 33;
const codeThree = (20 % 6) + 37;


let string = `You have received this message because you have been chosen to open an important vault.
 Here is the secret combination: ${codeOne}-${codeTwo}-${codeThree}`;

 alert(string);